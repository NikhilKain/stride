<?xml version="1.0" encoding="utf-8"?>
<!--
  Resizable from 2x1 up to 4x2. The widget itself picks a layout per size
  (SizeMode.Responsive in StrideWidget), so one provider covers all of them
  rather than shipping four entries the user has to choose between.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="40dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:maxResizeWidth="320dp"
    android:maxResizeHeight="200dp"
    android:targetCellWidth="3"
    android:targetCellHeight="1"
    android:updatePeriodMillis="1800000"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:previewLayout="@layout/widget_preview"
    android:description="@string/widget_description" />
